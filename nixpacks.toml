providers = ["python"]

[variables]
PYTHON_VERSION = "3.10"

[phases.setup]
nixPkgs = ["python310", "jdk17"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "uvicorn main:app --host 0.0.0.0 --port $PORT"